# jinza 后端

基于 Node.js + Express，部署于 Render。

## 本地开发

```bash
npm install
npm run dev
```

## 生产部署

Render 自动检测 `start` 脚本。

## 主要文件
- `index.js`：主入口
- `package.json`：依赖与脚本
- `render.yaml`：Render 部署配置
